<p>
  <label for="settings_statuses">Status IDs</label>
  <%= text_field_tag 'settings[statuses]', @settings['statuses'] %>
  <label for="settings_need_comment">Require comment on spent time</label>
  <%= check_box_field_tag 'settings[need_comment]', @settings['need_comment'] %>
</p>
